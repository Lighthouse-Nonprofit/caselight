CIF.DashboardsIndex = {
  // The personal 7-day agenda is server-rendered (and the org charts died with the
  // orphaned partials) — this page needs no JS. Registered so the dispatcher lookup
  // stays a clean no-op.
  init() {},
};
