.row
  .col-sm-12
    .ibox
      .ibox-title
        %h5= "#{t('.legacy_quarterly_reports_for')} #{entity_name(@case.client)}"
        .ibox-tools
          = link_to t('.back'), client_path(@case.client), { class: 'btn btn-outline-secondary button btn-back-default' }
      .ibox-content
        = datagrid_table @quarterly_reports_grid, html: {class: 'table cl-record-table pushing-top quarterly-reports'}

        = paginate @quarterly_reports_grid.assets
