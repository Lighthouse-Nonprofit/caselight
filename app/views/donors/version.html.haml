.row
  .col-12
    .card
      .card-body
        .row
          .col-4.col-md-2
            = link_to t('.back'), donors_path, class: 'btn btn-outline-secondary btn-back-default btn-back'
          .col-8.col-md-7.text-center
            %u= "#{t('.modification_of')} #{@donor.name}"
          .col-12.col-md-3
            = render 'shared/per_page_form'

        = render 'shared/version'
