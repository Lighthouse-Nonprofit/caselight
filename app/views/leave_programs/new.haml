= render 'clients/client_header', client: @client, active_tab: client_hub_active_tab
.row
  .col-12
    .ibox
      .ibox-title
        %h5= "#{@client.en_and_local_name} - #{t('.new_exit_program')}"
      = render 'form'
