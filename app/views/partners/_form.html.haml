= simple_form_for(@partner) do |f|
  .ibox-content
    = f.error_notification
    .form-inputs
      .row
        .col-12.col-sm-6.col-md-4= f.input :name, label: t('.name')
        .col-12.col-sm-6.col-md-4= f.input :contact_person_name, label: t('.contact_name')
        .col-12.col-sm-6.col-md-4= f.input :contact_person_email, label: t('.contact_email')
        .col-12.col-sm-6.col-md-4= f.input :contact_person_mobile, label: t('.contact_mobile')
        .col-12.col-sm-6.col-md-4= f.association :province, collection: @province, label: t('.province')
        .col-12.col-sm-6.col-md-4= f.input :address, input_html: { rows: '4' }, label: t('.address')
        .col-12.col-sm-6.col-md-4
          .mb-3.date.optional.partner_start_date
            %label.date.optional.form-label{ for: 'partner_start_date' } #{t('.start_date')}
            .input-group.date
              = f.text_field :start_date, class: 'date optional form-control date-picker', type: 'text', name: 'partner[start_date]', id: 'partner_start_date'
              %span.input-group-text
                %i.fa.fa-calendar-check-o
          = f.input :engagement, label: t('.engagement')
        .col-12.col-sm-6.col-md-4
          = f.input :organisation_type, label: t('.organisation_type')
          = f.input :affiliation, label: t('.affiliation')
        .col-12.col-sm-6.col-md-4
          = f.input :background, input_html: { style: 'height: 107px;' }, label: t('.background')
  .ibox-footer
    = f.submit t('save'), class: 'btn btn-primary form-btn'
    - if @partner.persisted?
      = link_to t('cancel'), partner_path(@partner), class: 'btn btn-outline-secondary form-btn'
    - else
      = link_to t('cancel'), partners_path, class: 'btn btn-outline-secondary form-btn'
